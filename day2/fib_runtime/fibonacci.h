#ifndef FIBONACCI_H
#define FIBONACCI_H

// WRITE YOUR CODE HERE
// to declare the enum and the function that outputs enum values from strings.
// You will also need to change this line to match the new signature that takes
// your enum as input
void print_fibonacci(const int fib_max);

#endif
